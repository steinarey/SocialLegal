# Copy to .env and fill in values, then `docker compose up -d`.
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=
INGEST_SECRET=change-me-for-ingest-endpoint
SESSION_SECRET=change-me-random-32-bytes
POSTGRES_PASSWORD=change-me-postgres
